# sq-server-commons

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build sq-server-commons` to build the library.

## Running unit tests

Run `nx test sq-server-commons` to execute the unit tests via [Jest](https://jestjs.io).
