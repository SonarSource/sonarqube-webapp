diff --git a/dist/rules/prefer-empty.js b/dist/rules/prefer-empty.js
index 5381b991e7890894a1902eb6e7e83ac7e7b7a1c2..e0ddcda66a0db3c8b6cd07d53ccbdfdaac67bd9f 100644
--- a/dist/rules/prefer-empty.js
+++ b/dist/rules/prefer-empty.js
@@ -21,7 +21,7 @@ const meta = exports.meta = {
 };
 const create = context => {
   function isNonEmptyStringOrTemplateLiteral(node) {
-    return !['""', "''", "``", "null"].includes((0, _context.getSourceCode)(context).getText(node));
+    return !['""', "''", "``", "null"].includes(context.sourceCode.getText(node));
   }
   return {
     [`BinaryExpression[left.property.name='innerHTML'][right.value=''][parent.callee.name='expect'][parent.parent.property.name=/toBe$|to(Strict)?Equal/]`](node) {
diff --git a/dist/rules/prefer-in-document.js b/dist/rules/prefer-in-document.js
index 5e90603494d0eacbef3cd2215ec39b03634ac572..482d0999c49fff2cc195b29e326bad09fdcaf7f6 100644
--- a/dist/rules/prefer-in-document.js
+++ b/dist/rules/prefer-in-document.js
@@ -152,7 +152,7 @@ const create = context => {
 
           // Remove any arguments in the matcher
           for (const argument of Array.from(matcherArguments)) {
-            const sourceCode = (0, _context.getSourceCode)(context);
+            const sourceCode = context.sourceCode;
             const token = sourceCode.getTokenAfter(argument);
             if (token.value === "," && token.type === "Punctuator") {
               // Remove commas if toHaveLength had more than one argument or a trailing comma
diff --git a/dist/rules/prefer-to-have-attribute.js b/dist/rules/prefer-to-have-attribute.js
index 9af75d79b7f6faa2cda880c858f02c7b1d47cb1c..14d09524d9bf8738f6855def1b8342a9ee11ebdc 100644
--- a/dist/rules/prefer-to-have-attribute.js
+++ b/dist/rules/prefer-to-have-attribute.js
@@ -28,11 +28,11 @@ const create = context => ({
     context.report({
       node: node.parent,
       message: `Use toHaveAttribute instead of asserting on getAttribute`,
-      fix: fixer => [fixer.removeRange([node.callee.object.range[1], node.range[1]]), fixer.replaceTextRange([node.parent.parent.property.range[0], node.parent.parent.parent.range[1]], `not.toHaveAttribute(${context.getSourceCode().getText(node.arguments[0])})`)]
+      fix: fixer => [fixer.removeRange([node.callee.object.range[1], node.range[1]]), fixer.replaceTextRange([node.parent.parent.property.range[0], node.parent.parent.parent.range[1]], `not.toHaveAttribute(${context.sourceCode.getText(node.arguments[0])})`)]
     });
   },
   [`CallExpression[callee.property.name='getAttribute'][parent.callee.name='expect'][parent.parent.property.name=/toContain$|toMatch$/]`](node) {
-    const sourceCode = (0, _context.getSourceCode)(context);
+    const sourceCode = context.sourceCode;
     context.report({
       node: node.parent,
       message: `Use toHaveAttribute instead of asserting on getAttribute`,
@@ -42,7 +42,7 @@ const create = context => ({
   [`CallExpression[callee.property.name='getAttribute'][parent.callee.name='expect'][parent.parent.property.name=/toBe$|to(Strict)?Equal/]`](node) {
     const arg = node.parent.parent.parent.arguments;
     const isNull = arg.length > 0 && arg[0].value === null;
-    const sourceCode = (0, _context.getSourceCode)(context);
+    const sourceCode = context.sourceCode;
     context.report({
       node: node.parent,
       message: `Use toHaveAttribute instead of asserting on getAttribute`,
@@ -69,7 +69,7 @@ const create = context => ({
       context.report({
         node: node.parent,
         message: `Use toHaveAttribute instead of asserting on hasAttribute`,
-        fix: fixer => [fixer.removeRange([node.callee.object.range[1], node.range[1]]), fixer.replaceText(node.parent.parent.property, `${node.parent.parent.parent.arguments[0].value === false ? "not." : ""}toHaveAttribute`), fixer.replaceText(node.parent.parent.parent.arguments[0], (0, _context.getSourceCode)(context).getText(node.arguments[0]))]
+        fix: fixer => [fixer.removeRange([node.callee.object.range[1], node.range[1]]), fixer.replaceText(node.parent.parent.property, `${node.parent.parent.parent.arguments[0].value === false ? "not." : ""}toHaveAttribute`), fixer.replaceText(node.parent.parent.parent.arguments[0], context.sourceCode.getText(node.arguments[0]))]
       });
     } else {
       context.report({
@@ -82,7 +82,7 @@ const create = context => ({
     context.report({
       node: node.parent,
       message: `Use toHaveAttribute instead of asserting on hasAttribute`,
-      fix: fixer => [fixer.removeRange([node.callee.object.range[1], node.range[1]]), fixer.replaceTextRange([node.parent.parent.property.range[0], node.parent.parent.parent.range[1]], `${node.parent.parent.property.name === "toBeFalsy" ? "not." : ""}toHaveAttribute(${context.getSourceCode().getText(node.arguments[0])})`)]
+      fix: fixer => [fixer.removeRange([node.callee.object.range[1], node.range[1]]), fixer.replaceTextRange([node.parent.parent.property.range[0], node.parent.parent.parent.range[1]], `${node.parent.parent.property.name === "toBeFalsy" ? "not." : ""}toHaveAttribute(${context.sourceCode.getText(node.arguments[0])})`)]
     });
   }
 });
diff --git a/dist/rules/prefer-to-have-class.js b/dist/rules/prefer-to-have-class.js
index 44ae965583f749b892acd0dac424160ddf351b5e..7bc717a084d30a0526244e68000f956a7480b389 100644
--- a/dist/rules/prefer-to-have-class.js
+++ b/dist/rules/prefer-to-have-class.js
@@ -41,9 +41,9 @@ const create = context => ({
       node: matcher,
       messageId,
       fix(fixer) {
-        return [fixer.removeRange([checkedProp.range[1], expectArg.range[1]]), fixer.replaceText(matcher, `${isTruthy ? "" : "not."}toHaveClass`), matcherArg ? fixer.replaceText(matcherArg, (0, _context.getSourceCode)(context).getText(classValue)) : fixer.insertTextBefore((0, _context.getSourceCode)(context).getTokenAfter(matcher, {
+        return [fixer.removeRange([checkedProp.range[1], expectArg.range[1]]), fixer.replaceText(matcher, `${isTruthy ? "" : "not."}toHaveClass`), matcherArg ? fixer.replaceText(matcherArg, context.sourceCode.getText(classValue)) : fixer.insertTextBefore(context.sourceCode.getTokenAfter(matcher, {
           skip: 1
-        }), (0, _context.getSourceCode)(context).getText(classValue))];
+        }), context.sourceCode.getText(classValue))];
       }
     });
   },
@@ -59,7 +59,7 @@ const create = context => ({
       fix(fixer) {
         //can't autofix here as it toHaveClass doesn't have a partial matcher / regex for class names.
         if (matcher.name === "toContain") return;
-        return [fixer.removeRange([classNameProp.object.range[1], expectArg.range[1]]), fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(classValue, (0, _context.getSourceCode)(context).getText(classValue))];
+        return [fixer.removeRange([classNameProp.object.range[1], expectArg.range[1]]), fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(classValue, context.sourceCode.getText(classValue))];
       }
     });
   },
@@ -93,7 +93,7 @@ const create = context => ({
         if (checkedProp.name === "classList" && matcher.name !== "toContain") {
           return;
         }
-        return [fixer.removeRange([classNameProp.range[1], checkedProp.range[1]]), fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(classValue, `${(0, _context.getSourceCode)(context).getText(classValue)}${matcher.name === "toContain" ? "" : ", { exact: true }"}`)];
+        return [fixer.removeRange([classNameProp.range[1], checkedProp.range[1]]), fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(classValue, `${context.sourceCode.getText(classValue)}${matcher.name === "toContain" ? "" : ", { exact: true }"}`)];
       }
     });
   },
@@ -113,7 +113,7 @@ const create = context => ({
       messageId,
       fix(fixer) {
         if (matcherArg.name !== "stringContaining") return;
-        return [fixer.removeRange([classNameProp.range[1], className.range[1]]), fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(node.arguments[0], `${(0, _context.getSourceCode)(context).getText(classValue)}`)];
+        return [fixer.removeRange([classNameProp.range[1], className.range[1]]), fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(node.arguments[0], `${context.sourceCode.getText(classValue)}`)];
       }
     });
   },
@@ -134,7 +134,7 @@ const create = context => ({
         if (className.name === "classList" && matcher.name !== "toContain") {
           return;
         }
-        return [fixer.removeRange([classNameProp.range[1], className.range[1]]), fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(classValue, `${(0, _context.getSourceCode)(context).getText(classValue)}${matcher.name === "toContain" ? "" : ", { exact: true }"}`)];
+        return [fixer.removeRange([classNameProp.range[1], className.range[1]]), fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(classValue, `${context.sourceCode.getText(classValue)}${matcher.name === "toContain" ? "" : ", { exact: true }"}`)];
       }
     });
   },
@@ -143,7 +143,7 @@ const create = context => ({
   [[`CallExpression[callee.object.callee.name=expect][callee.property.name=toHaveAttribute][arguments.0.type=/Literal/][arguments.1.type=/Literal$/]`, `CallExpression[callee.object.callee.name=expect][callee.property.name=toHaveProperty][arguments.0.type=/Literal/][arguments.1.type=/Literal$/]`].join(",")](node) {
     const matcher = node.callee.property;
     const [classArg, classValueArg] = node.arguments;
-    const classNameValue = context.getSourceCode().getText(classArg).slice(1, -1);
+    const classNameValue = context.sourceCode.getText(classArg).slice(1, -1);
     if (matcher.name === "toHaveAttribute" && classNameValue !== "class" || matcher.name === "toHaveProperty" && classNameValue !== "className") {
       return;
     }
@@ -155,7 +155,7 @@ const create = context => ({
       node: matcher,
       messageId,
       fix(fixer) {
-        return [fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(classArg, (0, _context.getSourceCode)(context).getText(classValueArg)), fixer.replaceText(classValueArg, `{ exact: true }`)];
+        return [fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(classArg, context.sourceCode.getText(classValueArg)), fixer.replaceText(classValueArg, `{ exact: true }`)];
       }
     });
   },
@@ -165,7 +165,7 @@ const create = context => ({
     //[callee.object.property.name=/toHaveAttribute|toHaveProperty/][arguments.0.value=class][arguments.1.type=/Literal$/]
     const matcher = node.callee.property;
     const [classArg, classValueArg] = node.arguments;
-    const classNameValue = context.getSourceCode().getText(classArg).slice(1, -1);
+    const classNameValue = context.sourceCode.getText(classArg).slice(1, -1);
     if (matcher.name === "toHaveAttribute" && classNameValue !== "class" || matcher.name === "toHaveProperty" && classNameValue !== "className") {
       return;
     }
@@ -177,7 +177,7 @@ const create = context => ({
       node: matcher,
       messageId,
       fix(fixer) {
-        return [fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(classArg, (0, _context.getSourceCode)(context).getText(classValueArg)), fixer.replaceText(classValueArg, `{ exact: true }`)];
+        return [fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(classArg, context.sourceCode.getText(classValueArg)), fixer.replaceText(classValueArg, `{ exact: true }`)];
       }
     });
   },
@@ -187,7 +187,7 @@ const create = context => ({
     const matcher = node.callee.property;
     const [classArg, classValue] = node.arguments;
     const classValueArg = classValue.arguments[0];
-    const classNameValue = context.getSourceCode().getText(classArg).slice(1, -1);
+    const classNameValue = context.sourceCode.getText(classArg).slice(1, -1);
     if (matcher.name === "toHaveAttribute" && classNameValue !== "class" || matcher.name === "toHaveProperty" && classNameValue !== "className") {
       return;
     }
@@ -199,7 +199,7 @@ const create = context => ({
       node: matcher,
       messageId,
       fix(fixer) {
-        return [fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(classArg, (0, _context.getSourceCode)(context).getText(classValueArg)), fixer.removeRange([classArg.range[1], classValue.range[1]])];
+        return [fixer.replaceText(matcher, "toHaveClass"), fixer.replaceText(classArg, context.sourceCode.getText(classValueArg)), fixer.removeRange([classArg.range[1], classValue.range[1]])];
       }
     });
   }
diff --git a/dist/rules/prefer-to-have-style.js b/dist/rules/prefer-to-have-style.js
index 76bf8d4ddaaf02b9ea8e7eed4fac86a74029ccbd..af36910a9fb7c3e8ae9aa6083aa3081286327f64 100644
--- a/dist/rules/prefer-to-have-style.js
+++ b/dist/rules/prefer-to-have-style.js
@@ -28,10 +28,10 @@ const create = context => {
     if (styleName.type === "Literal") {
       return camelCase(styleName.value);
     }
-    return `[${(0, _context.getSourceCode)(context).getText(styleName)}]`;
+    return `[${context.sourceCode.getText(styleName)}]`;
   }
   function getReplacementStyleParam(styleName, styleValue) {
-    return styleName.type === "Literal" ? `{${camelCase(styleName.value)}: ${context.getSourceCode().getText(styleValue)}}` : `${(0, _context.getSourceCode)(context).getText(styleName).slice(0, -1)}: ${styleValue.type === "TemplateLiteral" ? (0, _context.getSourceCode)(context).getText(styleValue).substring(1) : `${styleValue.value}\``}`;
+    return styleName.type === "Literal" ? `{${camelCase(styleName.value)}: ${context.sourceCode.getText(styleValue)}}` : `${context.sourceCode.getText(styleName).slice(0, -1)}: ${styleValue.type === "TemplateLiteral" ? context.sourceCode.getText(styleValue).substring(1) : `${styleValue.value}\``}`;
   }
   return {
     //expect(el.style.foo).toBe("bar");
@@ -43,7 +43,7 @@ const create = context => {
         node: node.property,
         message: "Use toHaveStyle instead of asserting on element style",
         fix(fixer) {
-          return [fixer.removeRange([node.object.range[1], styleName.range[1]]), fixer.replaceText(matcher, "toHaveStyle"), fixer.replaceText(styleValue, `{${styleName.name}:${context.getSourceCode().getText(styleValue)}}`)];
+          return [fixer.removeRange([node.object.range[1], styleName.range[1]]), fixer.replaceText(matcher, "toHaveStyle"), fixer.replaceText(styleValue, `{${styleName.name}:${context.sourceCode.getText(styleValue)}}`)];
         }
       });
     },
@@ -56,7 +56,7 @@ const create = context => {
         node: node.property,
         message: "Use toHaveStyle instead of asserting on element style",
         fix(fixer) {
-          return [fixer.removeRange([node.object.range[1], styleName.range[1]]), fixer.replaceText(matcher, "toHaveStyle"), fixer.replaceText(styleValue, `{${styleName.name}:${context.getSourceCode().getText(styleValue)}}`)];
+          return [fixer.removeRange([node.object.range[1], styleName.range[1]]), fixer.replaceText(matcher, "toHaveStyle"), fixer.replaceText(styleValue, `{${styleName.name}:${context.sourceCode.getText(styleValue)}}`)];
         }
       });
     },
@@ -68,7 +68,7 @@ const create = context => {
         node: node.property,
         message: "Use toHaveStyle instead of asserting on element style",
         fix(fixer) {
-          return [fixer.removeRange([node.object.range[1], node.property.range[1]]), fixer.replaceText(matcher, "toHaveStyle"), fixer.replaceText(styleName, styleName.type === "Literal" ? `{${camelCase(styleName.value)}: expect.anything()}` : (0, _context.getSourceCode)(context).getText(styleName))];
+          return [fixer.removeRange([node.object.range[1], node.property.range[1]]), fixer.replaceText(matcher, "toHaveStyle"), fixer.replaceText(styleName, styleName.type === "Literal" ? `{${camelCase(styleName.value)}: expect.anything()}` : context.sourceCode.getText(styleName))];
         }
       });
     },
@@ -80,7 +80,7 @@ const create = context => {
         node: node.property,
         message: "Use toHaveStyle instead of asserting on element style",
         fix(fixer) {
-          return [fixer.removeRange([node.object.range[1], node.property.range[1]]), fixer.replaceText(matcher, "toHaveStyle"), fixer.replaceText(styleName, styleName.type === "Literal" ? `{${camelCase(styleName.value)}: expect.anything()}` : (0, _context.getSourceCode)(context).getText(styleName))];
+          return [fixer.removeRange([node.object.range[1], node.property.range[1]]), fixer.replaceText(matcher, "toHaveStyle"), fixer.replaceText(styleName, styleName.type === "Literal" ? `{${camelCase(styleName.value)}: expect.anything()}` : context.sourceCode.getText(styleName))];
         }
       });
     },
@@ -142,7 +142,7 @@ const create = context => {
           if (!styleValue || !["Literal", "TemplateLiteral"].includes(styleValue.type)) {
             return null;
           }
-          return [fixer.removeRange([node.object.range[1], node.property.range[1]]), fixer.replaceText(matcher, "toHaveStyle"), fixer.replaceTextRange([styleName.range[0], styleValue.range[1]], `{${getReplacementObjectProperty(styleName)}: ${context.getSourceCode().getText(styleValue)}}`)];
+          return [fixer.removeRange([node.object.range[1], node.property.range[1]]), fixer.replaceText(matcher, "toHaveStyle"), fixer.replaceTextRange([styleName.range[0], styleValue.range[1]], `{${getReplacementObjectProperty(styleName)}: ${context.sourceCode.getText(styleValue)}}`)];
         }
       });
     },
@@ -157,7 +157,7 @@ const create = context => {
           if (!styleValue || !["Literal", "TemplateLiteral"].includes(styleValue.type)) {
             return null;
           }
-          return [fixer.removeRange([node.object.range[1], node.property.range[1]]), fixer.replaceText(matcher, "toHaveStyle"), fixer.replaceTextRange([styleName.range[0], styleValue.range[1]], `{${getReplacementObjectProperty(styleName)}: ${context.getSourceCode().getText(styleValue)}}`)];
+          return [fixer.removeRange([node.object.range[1], node.property.range[1]]), fixer.replaceText(matcher, "toHaveStyle"), fixer.replaceTextRange([styleName.range[0], styleValue.range[1]], `{${getReplacementObjectProperty(styleName)}: ${context.sourceCode.getText(styleValue)}}`)];
         }
       });
     }
diff --git a/dist/rules/prefer-to-have-text-content.js b/dist/rules/prefer-to-have-text-content.js
index 8d57ab595d6662d235bd113b3e6272a57edabc96..e82a3a6cc62c1de998ba4edad8577a45f1572570 100644
--- a/dist/rules/prefer-to-have-text-content.js
+++ b/dist/rules/prefer-to-have-text-content.js
@@ -22,12 +22,12 @@ const meta = exports.meta = {
 const create = context => ({
   [`MemberExpression[property.name='textContent'][parent.callee.name='expect'][parent.parent.property.name=/toContain$|toMatch$/]`](node) {
     const expectedArg = node.parent.parent.parent.arguments[0];
-    const expectedArgSource = (0, _context.getSourceCode)(context).getText(expectedArg);
+    const expectedArgSource = context.sourceCode.getText(expectedArg);
     context.report({
       node: node.parent,
       message: `Use toHaveTextContent instead of asserting on DOM node attributes`,
       fix: fixer => {
-        return [fixer.removeRange([node.object.range[1], node.property.range[1]]), fixer.replaceTextRange(node.parent.parent.property.range, "toHaveTextContent"), fixer.replaceTextRange(expectedArg.range, expectedArg.type === "Literal" ? expectedArg.regex ? expectedArgSource : new RegExp(expectedArg.value.toString().replace(/[.*+\-?^${}()|[\]\\]/g, "\\$&")) : `new RegExp(${expectedArgSource})`)];
+        return [fixer.removeRange([node.object.range[1], node.property.range[1]]), fixer.replaceTextRange(node.parent.parent.property.range, "toHaveTextContent"), fixer.replaceTextRange(expectedArg.range, expectedArg.type === "Literal" ? expectedArg.regex ? expectedArgSource : String(new RegExp(expectedArg.value.toString().replace(/[.*+\-?^${}()|[\]\\]/g, "\\$&"))) : `new RegExp(${expectedArgSource})`)];
       }
     });
   },
@@ -47,11 +47,11 @@ const create = context => ({
   },
   [`MemberExpression[property.name='textContent'][parent.callee.name='expect'][parent.parent.property.name='not'][parent.parent.parent.property.name=/toContain$|toMatch$/]`](node) {
     const expectedArg = node.parent.parent.parent.parent.arguments[0];
-    const expectedArgSource = (0, _context.getSourceCode)(context).getText(expectedArg);
+    const expectedArgSource = context.sourceCode.getText(expectedArg);
     context.report({
       node: node.parent,
       message: `Use toHaveTextContent instead of asserting on DOM node attributes`,
-      fix: fixer => [fixer.removeRange([node.object.range[1], node.property.range[1]]), fixer.replaceTextRange(node.parent.parent.parent.property.range, "toHaveTextContent"), fixer.replaceTextRange(expectedArg.range, expectedArg.type === "Literal" ? expectedArg.regex ? expectedArgSource : new RegExp(expectedArg.value.toString().replace(/[.*+\-?^${}()|[\]\\]/g, "\\$&")) : `new RegExp(${expectedArgSource})`)]
+      fix: fixer => [fixer.removeRange([node.object.range[1], node.property.range[1]]), fixer.replaceTextRange(node.parent.parent.parent.property.range, "toHaveTextContent"), fixer.replaceTextRange(expectedArg.range, expectedArg.type === "Literal" ? expectedArg.regex ? expectedArgSource : String(new RegExp(expectedArg.value.toString().replace(/[.*+\-?^${}()|[\]\\]/g, "\\$&"))) : `new RegExp(${expectedArgSource})`)]
     });
   }
 });
diff --git a/dist/rules/prefer-to-have-value.js b/dist/rules/prefer-to-have-value.js
index d67637c072e1586e76824884d51e0ffdd8ea404d..ec5fb5586558ebc33ea78aa9e4994c1fb0162118 100644
--- a/dist/rules/prefer-to-have-value.js
+++ b/dist/rules/prefer-to-have-value.js
@@ -50,7 +50,7 @@ const create = context => {
           messageId,
           node,
           fix(fixer) {
-            return [fixer.remove((0, _context.getSourceCode)(context).getTokenBefore(valueProp)), fixer.remove(valueProp), fixer.replaceText(matcher, "toHaveValue")];
+            return [fixer.remove(context.sourceCode.getTokenBefore(valueProp)), fixer.remove(valueProp), fixer.replaceText(matcher, "toHaveValue")];
           }
         });
       }
@@ -67,7 +67,7 @@ const create = context => {
           messageId,
           node,
           fix(fixer) {
-            return [fixer.removeRange([(0, _context.getSourceCode)(context).getTokenBefore(valueProp).range[0], valueProp.range[1]]), fixer.replaceText(matcher, "toHaveValue")];
+            return [fixer.removeRange([context.sourceCode.getTokenBefore(valueProp).range[0], valueProp.range[1]]), fixer.replaceText(matcher, "toHaveValue")];
           }
         });
       }
